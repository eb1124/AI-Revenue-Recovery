import { EmptyState } from '../primitives'

export function AuditPage() {
  return <EmptyState title="The Audit log isn't built yet." description="This will show the hash-chained log, verify, and export." />
}
