import { EmptyState } from '../primitives'

export function WorldPage() {
  return <EmptyState title="World isn't built yet." description="This will hold the simulator controls and run launcher." />
}
