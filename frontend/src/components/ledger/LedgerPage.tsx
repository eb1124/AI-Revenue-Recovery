import { EmptyState } from '../primitives'

export function LedgerPage() {
  return <EmptyState title="The Ledger isn't built yet." description="This will show results — agent vs baseline vs holdout." />
}
