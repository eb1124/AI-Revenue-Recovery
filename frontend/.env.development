# Frontend-only development: serve fixture data via MSW instead of a real backend.
# Flip to false once the FastAPI backend exists (section 10.14).
VITE_USE_MOCKS=false
VITE_API_BASE_URL=http://localhost:8000
