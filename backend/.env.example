# Copy to .env. Matches docker-compose.yml's postgres service.
DATABASE_URL=postgresql+psycopg://hold:hold@localhost:5432/hold

# Emergency fallback if Postgres won't cooperate on-site (section 5.1):
# DATABASE_URL=sqlite:///./hold.db

ANTHROPIC_API_KEY=
